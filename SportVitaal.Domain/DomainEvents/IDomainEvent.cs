namespace SportVitaal.Domain.DomainEvents
{
    /// <summary>
    /// Marker interface for domain events produced by aggregates.
    /// Aggregates should collect events for the application layer to dispatch.
    /// </summary>
    public interface IDomainEvent
    {
    }
}

