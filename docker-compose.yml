services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: sportvitaal-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP
      - "8025:8025" # Web UI -> http://localhost:8025
